export enum Actions {
  Add = 'add',
  Edit = 'edit',
  Delete = 'delete',
}

export enum Fields {
  FirstName = 'firstName',
  LastName = 'lastName',
  Editing = 'editing',
  Books = 'books',
}

export enum Entities {
  Author = 'author',
  Book = 'book',
}
