export enum Actions {
  Add = 'add',
  Edit = 'edit',
  Delete = 'delete',
}

export enum Fields {
  Editing = 'editing',
  Books = 'books',
}

export enum Entities {
  Author = 'author',
  Book = 'book',
}
